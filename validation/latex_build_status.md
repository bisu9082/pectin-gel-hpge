# LaTeX build status

Checked on 2026-05-31.

The revised LaTeX files were not compiled in this workspace because
`pdflatex`, `xelatex`, `lualatex`, `latexmk`, and `tectonic` were not
available on the local PATH or in the checked common installation paths.

Static validation completed successfully:

- all cited keys have bibliography items;
- all five manuscript figure files exist;
- all five highlights are 85 characters or fewer;
- braces match in `main.tex`.

Before submission, compile the TeX files in Overleaf or a local TeX
distribution and inspect table width, line breaks, and figure placement.
